
    // Arithmetic Operators :
    
    // + (addition)
    // - (substraction)
    // / (division)
    // * (multiplication)

    // % (modulus)
   // ++ (increment)
   // -- (decrement)

#include <stdio.h>
int main(){
// 0.5
    int first = 4;
    int second = 2;

    // int result = first % second; // () Tuples 0.0 %.1lf

    first--;

    // printf("The result is : %d", result);

    printf("result of first after increment : %i", first);
    return 0;
}