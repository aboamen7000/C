#include <stdlib.h>
#include <stdio.h>

/**
 * main - print names then add new line.
 * @argc: argc counter numb
 * @argv: args arrays
 * @__attribute__((unused)): compile functions with unused variables
 * Return: 0
 */

int main(int argc __attribute__((unused)), char *argv[])
{
printf("%s\n", *argv);
return (0);
}
